# Core dependencies for llama-cpp-bench
requests>=2.28.0  # For HTTP requests to llama.cpp server
datasets>=2.0.0  # For loading HumanEval dataset
pandas>=1.5.0  # For data handling and CSV operations
matplotlib>=3.5.0  # For plotting
seaborn>=0.12.0  # For advanced plotting
nltk>=3.8  # For BLEU score calculation (optional, falls back to sacrebleu)
sacrebleu>=2.0.0  # Alternative BLEU score library (optional fallback)
tenacity>=8.0.0  # For retry logic on HTTP requests to LLM server

# Note: NLTK resources (punkt_tab) will be auto-downloaded on first use
# If you want to download manually: python -c "import nltk; nltk.download('punkt_tab')"
